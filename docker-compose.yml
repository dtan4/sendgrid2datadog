version: '2'
services:
  web:
    build: .
    environment:
      - DOGSTATSD_HOST=dogstatsd
    ports:
      - 9292:9292
  dogstatsd:
    image: datadog/docker-dd-agent:latest-dogstatsd
    environment:
      - API_KEY=${DATADOG_API_KEY}
